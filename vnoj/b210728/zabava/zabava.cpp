
#include <iostream>
using namespace std;

const int nlim = 1e6+7, mlim = 107, klim = 507;
int n, m, k;
// trans[i] chỉ phòng mà ngày thứ i sẽ có sinh viên chuyển vào
int trans[nlim];

int main() {
	cin >> n >> m >> k;
	for (int i = 0; i < n; i++) cin >> trans[i];
	//
	cin >> n;
	return 0;
}
