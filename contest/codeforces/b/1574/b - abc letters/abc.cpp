#include <iostream>
using namespace std;

#define u64 unsigned long long
u64 a, b, c, m;

bool calc() {

	return 0;
}

int main() {
	ios_base::sync_with_stdio(false); cin.tie(0);
	int t; cin >> t;
	while (t--) {
		cin >> a >> b >> c >> m;
		if (calc()) {
			cout << "YES\n";
		} else {
			cout << "NO\n";
		}
	}
	return 0;
}
