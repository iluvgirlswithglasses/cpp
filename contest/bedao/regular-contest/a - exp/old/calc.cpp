#include <iostream>
#include <string>
using namespace std;

unsigned long long a, b, x, y;

string calc() {
	
}

int main() {
	ios_base::sync_with_stdio(false); cin.tie(0);
	int q; cin >> q;
	while (q--) {
		cin >> a >> b >> x >> y;
		cout << calc() << "\n";
	}
	return 0;
}
